filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/filebeat/logs/containers/*/*.log
    # fields:
    #   type: docker
    # json.message_key: log

output.logstash:
  hosts: ["logstash:5044"]